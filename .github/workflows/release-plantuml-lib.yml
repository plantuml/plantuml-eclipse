name: PlantUML library release for Eclipse

on:
  push:
    branches:
      - automate-releases
  # Triggered by external repository_dispatch event (from plantuml repo)
  #repository_dispatch:
  #  types: [plantuml-release]
  # Allow manual triggering via GitHub UI
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4
      - name: Clone GitHub pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: build/gh-pages
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      #- name: Build latest PlantUML lib update site
      #  run: gradle buildPlantUmlLibUpdateSite
      - name: Add latest PlantUML lib to composite update site
        # TODO: for now we only prepare the git commit for publishing the update site at GitHub pages
        run: gradle gitCommitPlantUmlLibUpdateSiteToGhPages
